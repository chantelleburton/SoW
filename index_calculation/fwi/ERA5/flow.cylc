[scheduler]
    allow implicit tasks = True

[task parameters]
    start_year = 1979, 1989, 1999, 2009, 2019
    wind_stat = mean#, max
    rh_stat = mean#, minimum

[scheduling]
    [[graph]]
        R1 = """
            era5_fwi_calculation<start_year, wind_stat, rh_stat>
        """

[runtime]
    [[era5_fwi_calculation<start_year, wind_stat, rh_stat>]]
        script = """
        set -eux
        conda activate sowf
        cd /data/users/bob.potts/StateOfFires_2025-26/code/index_calculation/fwi/ERA5/
        echo "Running start_year=${CYLC_TASK_PARAM_start_year} wind_stat=${CYLC_TASK_PARAM_wind_stat} rh_stat=${CYLC_TASK_PARAM_rh_stat}"
        python era5_fwi_calculation.py
        """

        platform = spice

        [[[directives]]]
            --mem = 150G
            --partition = cpu-long
            --time = 720
            --cpus-per-task = 4
            --ntasks = 1